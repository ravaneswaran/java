# ----------------------------------------------------
# GENERAL
# ----------------------------------------------------
user                = admin
pid-file            = /var/lib/mysql/xiomi-laptop.pid
socket              = /var/run/mysqld/mysqld.sock
datadir             = /var/lib/mysql
symbolic-links      = 0
skip-name-resolve   = 1         # avoid DNS lookups for connections
max_connections     = 200       # depends on your connection pool size
wait_timeout        = 28800     # (8 hours) for pooled connections
interactive_timeout = 28800

# ----------------------------------------------------
# LOGGING
# ----------------------------------------------------
slow_query_log      = 1
slow_query_log_file = /var/lib/mysql/xiomi-laptop-slow.log
long_query_time     = 1
log_error           = /var/log/mysql/error.log

# ----------------------------------------------------
# STORAGE ENGINE (InnoDB)
# ----------------------------------------------------
default_storage_engine = InnoDB
innodb_buffer_pool_size = 4G          # ~50-70% of system RAM (for dedicated DB)
innodb_buffer_pool_instances = 4
innodb_log_file_size       = 1G
innodb_log_buffer_size     = 256M
innodb_flush_log_at_trx_commit = 1    # safest (ACID); use 2 for slightly better perf
innodb_flush_method        = O_DIRECT
innodb_file_per_table      = 1
innodb_read_io_threads     = 4
innodb_write_io_threads    = 4
innodb_io_capacity         = 2000
innodb_flush_neighbors     = 0        # good for SSDs

# ----------------------------------------------------
# CONNECTION & THREADS
# ----------------------------------------------------
thread_cache_size          = 64
table_open_cache           = 4096
open_files_limit           = 65535

# ----------------------------------------------------
# QUERY CACHE (only for MySQL < 8.0)
# ----------------------------------------------------
query_cache_type           = 0
query_cache_size           = 0
