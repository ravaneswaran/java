package rave.code.tech.analysis.trend;

public class UpTrend extends AbstractTrend{

    public UpTrend(double currentHigh, double currentLow, double previousHigh, double previousLow) {
        super(currentHigh, currentLow, previousHigh, previousLow);
    }
}
